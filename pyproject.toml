[project]
name = "loclm"
version = "0.1.2"
description = "localized language model specifically designed to be domain expert (instead of expert of everything) and very light weight"
authors = [
    {name = "Utkarsh (saraswat.utk@gmail.com)"}
]
license = {text = "Custom"}
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy (>=2.3.4,<3.0.0)",
]
[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]
ipykernel = "^7.1.0"

